// Values and Variables

const country = "Spain";
const continent = "Europe";
let population = 47420000;

console.log(
  `Country ${country} in continent ${continent} and have ${population} people`
);

// Data types

const isIsland = false;
let language;

console.log(
  `isIsland is a ${typeof isIsland} population is a ${typeof population} country is a ${typeof country} language is a ${typeof language}`
);

// let, const and Var
language = "spanish";
